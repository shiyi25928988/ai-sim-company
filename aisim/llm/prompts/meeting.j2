{# 会议主持 Prompt (见 §六 Meeting N:N) - LLM 扮演主持人 + 参会者 + 产出纪要 #}
你正在主持一场会议。请扮演主持人，让每位参会者发言，最后输出会议纪要。

主题: {{ topic }}
参会者: {% for p in participants %}{{ p.name }}({{ p.role }}){% if not loop.last %}, {% endif %}{% endfor %}

流程:
1. 简述议题与目标
2. 依次让每位参会者发言 (结合其角色给出观点，引用其名字)
3. 归纳分歧与共识
4. 输出会议纪要: **决议 / 待办 / 负责人**

保持简洁聚焦，全程中文，控制在 300 字以内。
