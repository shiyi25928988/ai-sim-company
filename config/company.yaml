ceo:
  agent_id: ceo-alex
  department: Executive
  name: Alex
  personality:
    agreeableness: 0.6
    conscientiousness: 0.9
    extraversion: 0.8
    neuroticism: 0.3
    openness: 0.85
  role: ceo
  salary: 0
company:
  business_description: Build a CLI tool for developers. Ship fast, charge 20 USD/mo.
  initial_capital: 500000
  monthly_budget: 80000
  name: Demo AI Co.
llm:
  api_key: ${LLM_API_KEY}
  base_url: ${LLM_BASE_URL}
  claude_code_enabled: true
  daily_budget: 2000000
  default_model: ${LLM_MODEL}
  enable_tools: ${LLM_TOOLS_ENABLED}
  max_iters: ${LLM_MAX_ITERS}
  provider: ${LLM_PROVIDER}
  routing: {}
simulation:
  agent_step_delay_ms: ${AGENT_STEP_DELAY_MS}
  agent_think_every: ${AGENT_THINK_EVERY}
  auto_start: ${SIM_AUTO_START}
  tick_interval_ms: ${TICK_INTERVAL_MS}
