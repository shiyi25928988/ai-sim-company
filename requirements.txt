# Runtime dependencies for ai-sim-company.
# Backend (Company Hub + Agent runtime) is Python 3.12.
fastapi>=0.115
python-multipart>=0.0.7   # FastAPI UploadFile (skill package upload)
mcp>=1.0                  # Model Context Protocol client (agent MCP tools)
uvicorn[standard]>=0.30
# (Redis removed - state is now in-memory + SQLite)
httpx>=0.27
pyyaml>=6.0
python-dotenv>=1.0   # 本地 uvicorn 直跑时自动加载 .env
jinja2>=3.1
# (Docker/Hermes removed - simulated mode only, no containers)
