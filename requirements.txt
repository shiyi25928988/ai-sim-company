# Runtime dependencies for ai-sim-company.
# Backend (Company Hub + Agent runtime) is Python 3.12.
fastapi>=0.115
python-multipart>=0.0.7   # FastAPI UploadFile (skill package upload)
mcp>=1.0                  # Model Context Protocol client (agent MCP tools)
uvicorn[standard]>=0.30
# redis[hiredis] provides the async client used everywhere (redis.asyncio).
# NOTE: the design doc names the deprecated `aioredis`; we use its successor
# `redis.asyncio` (same maintainer, merged in redis-py >=4.2).
redis[hiredis]>=5.0
httpx>=0.27
pyyaml>=6.0
python-dotenv>=1.0   # 本地 uvicorn 直跑时自动加载 .env
jinja2>=3.1
# Docker API client - Company Hub orchestrates agent containers with this.
aiodocker>=0.21
# Hermes Agent (Nous Research) - the underlying agent loop/memory/skills/LLM
# abstraction. Imported as `from hermes import HermesRuntime`. Available inside
# agent containers; pin/replace once the upstream package is finalized.
hermes-agent>=0.1
